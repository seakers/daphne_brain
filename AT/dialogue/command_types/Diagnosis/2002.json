{
  "type": "db_query",
  "params":
  [
    { "name": "anomaly_id", "type": "anomaly_id", "options": "", "from_context": false, "mandatory": true }
  ],
  "query":
  {
    "always": "session.query(models.ECLSSCause).join(models.symptom_to_cause).join(models.ECLSSAnomalies).filter(models.ECLSSAnomalies.id == ${anomaly_id})",
    "opt": [],
    "end": "",
    "results": [
      {
        "result_type": "list",
        "result_fields": {
          "cause": "row.description"
        }
      }
    ]
  },
  "voice_response": [
    {
      "type": "list",
      "begin": "The potential causes of anomaly ${anomaly_id} are: ",
      "repeat": "${cause}",
      "end": "."
    }
  ],
  "visual_response": [
    {
      "type": "text",
      "from": "list",
      "begin": "The potential causes of anomaly ${anomaly_id} are: ",
      "repeat": "${cause}",
      "end": "."
    }
  ]
}
